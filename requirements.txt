# v3/all.ipynb — core (install all of these for the main pipeline)
pandas>=2.0
numpy>=1.24
matplotlib>=3.7
seaborn>=0.13
scipy>=1.10
scikit-learn>=1.3
hdbscan>=0.8.33
requests>=2.28
tqdm>=4.65

# Optional — extension cells skip if missing
# §12 GSEA: place msigdb.gmt next to data.csv or set MSIGDB_GMT (MSigDB .gmt from https://www.gsea-msigdb.org/gsea/msigdb/human/collections.jsp )
logomaker>=0.8
gseapy>=1.0
networkx>=3.0
# Optional §14a: 2D embedding from adjacency (falls back to sklearn SpectralEmbedding if absent)
# umap-learn>=0.5
upsetplot>=0.8
